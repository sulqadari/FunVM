var truthy = true;
var falsey = false;

if (truthy || falsey) {
	var prefix = "inside ";
	println(prefix + "top-level");

	if (falsey || truthy) {
		println(prefix + "first nested level");

		if (truthy && falsey)
			println("Must skip this level");
		else {
			println("'truthy && falsey' returned false");

			if (falsey && truthy)
				println("Must skip this level");
			else
				println("'falsey && truthy' returned false");
		}
	}
}

if (truthy && falsey) {
	var prefix = "inside ";
	println(prefix + "top-level");

	if (falsey || truthy) {
		println(prefix + "first nested level");

		if (truthy && falsey)
			println("Must skip this level");
		else {
			println("'truthy && falsey' returned false");

			if (falsey && truthy)
				println("Must skip this level");
			else
				println("'falsey && truthy' returned false");
		}
	}
} else
	println("Jump over the second if statement");

if (truthy && falsey) {
	var prefix = "inside ";
	println(prefix + "top-level");

	if (falsey || truthy) {
		println(prefix + "first nested level");

		if (truthy && falsey)
			println("Must skip this level");
		else {
			println("'truthy && falsey' returned false");

			if (falsey && truthy)
				println("Must skip this level");
			else
				println("'falsey && truthy' returned false");
		}
	}
} else
	println("Jump over the third if statement");
