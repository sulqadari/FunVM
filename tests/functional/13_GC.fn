fun global() {
	var local = "string";
	
	fun closure() {
		var internal = local + " become internal";
		return internal;
	}

	return closure;
}

for (var i = 0; i < 1000; i = i + 1) {
	var loop = global();
	println(" inside loop.");
	println(loop);
}

class MainClass {

}

// Commenting out previous for() loop test results
// in segmentation fault in this for() loop.
// After few attempts (commenting/uncommenting the above mentioned code)
// Terminal printed out "emergency termination"
for (var i = 0; i < 100; i = i + 1) {
	MainClass();
}